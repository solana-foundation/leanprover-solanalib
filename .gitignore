/.lake/
/build/
.cache/
.DS_Store

# Claude Code per-machine settings (skills under .claude/skills/ ARE committed)
/.claude/settings.local.json

# Personal Lean playground — never committed
/scratch/
